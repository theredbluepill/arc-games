# Registry GIF batch — inventory (tracker scope)

- **Tracker stems**: 201 (`ll01` … `fe02` in `GAMES.md` order) — see `registry_gif_tracker_stems.txt`.
- **Dispatch CSV**: `registry_gif_dispatch.csv` (249 rows); tracker stems all present.
- **High-risk (tracker ∩ generic ∩ ACTION6/click)**: 83 — see `registry_gif_high_risk_tracker.txt`.
- **gif_inventory.py**: 249 table games; 0 empty previews; 0 missing linked GIF files on disk (pre-batch).
